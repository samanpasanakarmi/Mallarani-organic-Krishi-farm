# Mallarani Agricultural Farm — completion pass v1

## Fixed
- Normalized `Images/` to `images/` so deployment works on case-sensitive Linux hosts.
- Fixed `updates.js`: it previously redeclared `farmProducts`; it now correctly defines `farmUpdates`.
- Replaced the remote stock hero background with the project's own `farm-1.jpg`.
- Improved header/mobile navigation, visual hierarchy, buttons, cards, gallery hover states, and responsive styling.
- Clarified that `admin.html` and `farm-admin.html` are local code-generator/editor utilities, not authenticated admin panels.

## Preserved
- English / Nepali language switching.
- Product category filters and availability cards.
- WhatsApp ordering/contact workflow.
- Price list, delivery/pickup, farm gallery, reviews, FAQ, Google Maps, SEO and analytics integration.

## Before public launch
Verify the phone/WhatsApp number, email, map link, prices, availability, visiting hours and any claims such as “organic”, “chemical-free”, or “100% natural” with the farm owner. Static websites cannot provide secure authenticated administration without a backend/service.
